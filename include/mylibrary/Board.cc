//
// Created by Venyatha MANNE on 4/23/20.
//

#include <mylibrary/Board.h>
#include <vector>

namespace mylibrary {

/*
Board::Board(int boardSize) {
  board_size_ = boardSize;

  std::vector<int> vec(board_size_,0);
  grid_(board_size_, vec);

  int count = 0;
  for (int i = 0; i < boardSize; i++) {
    for (int j = 0; j < boardSize; j++) {
      grid_[i][j] = count;
      count++;
    }
  }
  */

}



}  // namespace mylibrary